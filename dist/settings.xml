<?xml version="1.0" encoding="UTF-8" ?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">

    <servers>
        <!-- Nexus server credentials injected from action inputs -->
        <server>
            <id>${env.INPUT_SERVER_ID}</id>
            <password>${env.INPUT_NEXUS_PASSWORD}</password>
            <username>${env.INPUT_NEXUS_USERNAME}</username>
        </server>
    </servers>

    <profiles>
        <!-- GPG signing configuration -->
        <profile>
            <id>gpg</id>
            <properties>
                <gpg.executable>gpg</gpg.executable>
                <gpg.passphrase>${env.INPUT_GPG_PASSPHRASE}</gpg.passphrase>
            </properties>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
        </profile>
    </profiles>

</settings>
